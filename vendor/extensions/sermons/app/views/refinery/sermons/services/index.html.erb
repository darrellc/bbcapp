<% content_for :body do %>
  <ul id="services">
    <% @services.each do |service| %>
      <li>
        <%= link_to service.name, refinery.sermons_service_path(service) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
